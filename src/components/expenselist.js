import React from 'react';

const ExpenseList = () => {
  // Fetch and display expenses from local storage

  return (
    <div>
      <h2>Expenses</h2>
      {/* Display expenses here */}
    </div>
  );
};

export default ExpenseList;
